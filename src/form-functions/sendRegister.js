const sendRegister = (registerUsername, registerEmail, registerPassword, registerConfirmPassword) => {
    alert(registerUsername + ' ' + registerEmail + ' ' + registerPassword + ' ' + registerConfirmPassword);
}

export default sendRegister;
